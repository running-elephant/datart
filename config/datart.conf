# this file has the highest priority, if val is not blank, then will replace the config

# datasource config
datasource.ip=
datasource.port=
datasource.database=
datasource.username=
datasource.password=

# server
server.port=8080
server.address=0.0.0.0
server.ssl.enabled=false
server.ssl.key-store=
server.ssl.key-store-password=
server.ssl.keyStoreType=PKCS12
server.ssl.keyAlias:

# datart config. can change to https if ssl is enabled
datart.address=http://127.0.0.1
datart.send-mail=false
datart.webdriver-path=http://127.0.0.1:4444/wd/hub
